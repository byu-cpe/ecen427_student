# Virtualizing the CPU and Memory

`cpu.c` and `mem.c` from Chapter 2 of *Operating Systems: Three Easy Pieces*.
See the comments at the top of each file for how to run them.

```
make          # builds cpu and mem
```
